# NYUCTF-style solver environment for the agent shell (bash in /workspace).
# Omitted: ubuntu-desktop (full GUI; huge and unnecessary in a headless workbench).
# netcat -> netcat-openbsd (provides ``nc`` on Ubuntu 22.04).
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    vim \
    cmake \
    libgtk2.0-dev \
    pkg-config \
    libavcodec-dev \
    libavformat-dev \
    libswscale-dev \
    python3-dev \
    python3-numpy \
    python3-pip \
    libssl-dev \
    libffi-dev \
    libtbb2 \
    libtbb-dev \
    libjpeg-dev \
    libpng-dev \
    libtiff-dev \
    bc \
    bsdmainutils \
    curl \
    netcat-openbsd \
    python3-venv \
    qemu-user \
    qemu-user-static \
    radare2 \
    sagemath \
  && rm -rf /var/lib/apt/lists/*

RUN pip3 install --no-cache-dir --upgrade pip \
  && pip3 install --no-cache-dir pwntools ipython gmpy2